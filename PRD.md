# PRD：每日打卡清单（habit-checkin）· MVP v0.1

| 项 | 内容 |
| --- | --- |
| 项目 | habit-checkin（每日打卡清单） |
| 版本 | v0.1 · Day 4（第 1 周） |
| 范围 | Day 7 之前能做完的 MVP |
| 状态 | 待评审 |
| 上游文档 | `research.md`（Day 3 需求研究） |
| 技术选型 | Day 5 决定，本文档不涉及 |

---

## 一、背景与目标

**一句话**：给 Henry 自己用的一个**手机网页清单** —— 打开就能看到今天要做的事、做完勾掉；**有时间点的事（作业截止、赶火车）交给手机自带日历去提醒**，本工具只管打卡这一件事。

**目标**

| 编号 | 目标 | 怎么算达成 |
| --- | --- | --- |
| G1 | 每天早晨能快速列完今天要做的事并逐项勾掉 | 打开到勾完 ≤ 10 秒（第七节 A1） |

**非目标**（本项目不解决）：**提醒功能**（时间与提前量）—— 手机自带日历 / 提醒 App 原生就支持，且关着 App 也能响，网页做只会更麻烦（决策 D8）、团队协作、习惯养成激励、跨设备同步、数据分析。

> **为什么砍掉提醒**：2026-09-19 拍板。原本设计了 F2（网页提醒列表）和 F6（导出 `.ics`），
> 但你要"主要在手机上用、不要太麻烦"。手机上"关着也能提前提醒"只有系统日历做得到 ——
> 网页通知要页面活着，Web Push 需要后端。既然日历本来就在手机上、还更好用，就没必要在网页里再做一遍。

---

## 二、用户与使用场景

**用户**：一个人 —— Henry 本人。**主要在手机上用**（手机浏览器打开，建议「添加到主屏幕」），电脑为辅。

> **这条改过**：原写的是"Windows，浏览器打开"。2026-09-19 你说主要用手机，前提就变了 ——
> 手机浏览器打不开本地 `file://` 文件，所以本工具**必须有一个网址**（见第七节 D1；托管方式见第十节 D10）。

| 场景 | 时间 | 动作 | 关键要求 |
| --- | --- | --- | --- |
| A · 早晨列清单 | 起床后 | 手机打开网页 → 看今天要做什么 → 勾掉已做的 → 补两条新的 | 一步到位，不能要我先选视图或先登录 |
| B · 白天 | 随时 | **不打开本工具** —— 有时间点的事由手机日历响 | 本工具不参与，见 D8 |
| C · 晚上收尾 | 睡前 | 扫一眼还剩什么 → 把昨天没做完的挪到明天或删掉 | 未完成的事不能悄悄消失 |

**约束**：每天至少打开一次；**打开成本决定成败** —— 行为自动化中位数是 66 天（Harkin 等，138 项研究），每天多花 20 秒，累计起来就是放弃的理由。这条是所有取舍的最高判据。

---

## 三、功能范围（MVP 做什么）

> **F2（提醒列表）、F5（提醒边界）、F6（日历导出）已按决策 D8 整块砍掉**（2026-09-19）。
> 编号保留空缺，方便翻历史记录时对得上。MVP 现在只剩三组：**F1 今日清单 / F3 逾期区 / F4 数据存储**。

### F1 · 今日清单

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| F1.1 | 列表展示 | 今天要做的事，一件事一行 |
| F1.2 | 勾选 / 取消 | 点一下圆点切换完成状态 |
| F1.3 | 今日进度 | 顶部显示 `3/5`，只统计**今天**的项 |
| F1.4 | 添加 | 输入框输入标题，回车即加入今日 |
| F1.5 | 删除 | 每行右侧删除按钮 |

**F1 用户动作 → 可观察结果**

| 用户动作 | 可观察结果（眼睛能直接看到的） |
| --- | --- |
| 点一下某行左侧的空心圆 | 圆点变实心、标题划掉；顶部进度分子 +1（3/5 → 4/5） |
| 再点一次实心圆 | 回到空心，标题恢复，进度分子 −1 |
| 在输入框打字后按回车 | 新项出现在今日清单末尾；输入框清空且保持聚焦，可继续输入下一条 |
| 点某行右侧删除按钮 | 该行立即消失，进度分母 −1（3/5 → 3/4） |
| 什么都不输直接回车 | 没有任何反应，输入框保持原样（对应 B1） |

### F3 · 逾期区（今日决策）

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| F3.1 | 集中展示 | 昨天及更早**未完成**的清单项，堆在页面顶部「逾期」区 |
| F3.2 | 两个动作 | 挪到今天 / 删除，只有这两种 |
| F3.3 | 不计入进度 | 逾期项不算进今日 `n/m` 的分母，避免进度数字虚高 |
| F3.4 | 无则不显示 | 没有逾期项时该区块完全隐藏 |

**F3 用户动作 → 可观察结果**

| 用户动作 | 可观察结果（眼睛能直接看到的） |
| --- | --- |
| 第二天打开页面（昨天有没勾的项） | 顶部出现「逾期(n)」区块；该项**不在**今日清单里，也不进今日进度分母 |
| 点逾期项右侧「挪到今天」 | 该条从逾期区移到今日清单，进度分母 +1（3/5 → 3/6） |
| 点逾期项右侧「删除」 | 该条彻底消失，不进今日清单、也不再出现在逾期区 |
| 把逾期区最后一条处理掉 | 整个逾期区块隐藏，页面顶部直接回到日期 + 进度那一行 |

> **为什么是逾期区而不是自动顺延**，分两半说清楚，一半有据、一半是推断：
>
> - **有据的一半**：自动处理会把事弄丢。滴答清单用户反馈"每日重复任务当天没完成，第二天就不延续了"（`research.md` 第四节③）—— 事就这么没了，这是最糟的结果。
> - **推断的一半**：自动顺延会让清单越积越长、几十条未完成堆在一起让人不想打开。**这条是我们的推断，`research.md` 里没有直接证据**，Day 7 用起来觉得不对可以回头改。
>
> 两半合起来才推出逾期区：保留"今日清单每天是干净的"（学 Streaks 的打开即达），同时保证没有事被悄悄丢掉。

### F4 · 数据存储

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| F4.1 | 本地持久化 | 浏览器 localStorage，刷新、关闭浏览器都不丢 |
| F4.2 | 首次引导 | 首次打开给 3 条示例项，可一键清空 |

**F4 用户动作 → 可观察结果**

| 用户动作 | 可观察结果（眼睛能直接看到的） |
| --- | --- |
| 勾几项后按 F5 刷新 | 勾选状态、进度数字、清单内容全部保持原样 |
| 关掉浏览器第二天再打开 | 昨天的数据还在（对应 A7） |
| 第一次打开这个网页 | 看到 3 条示例项，旁边有「清空示例」 |

---

## 四、明确不做（砍掉了什么，为什么砍）

| 砍掉的功能 | 为什么砍 | 依据 |
| --- | --- | --- |
| **提醒（时间 / 提前量 / 通知）** | 手机自带日历 / 提醒 App 原生就支持提前量，关着 App 也能响；网页做反而更麻烦（要部署、要 PWA 授权，还不一定可靠） | **D8**（2026-09-19 拍板） |
| 游戏化（积分 / 等级 / 成就 / 宠物） | 激励层会反客为主，变成第二个要维护的系统 | Habitica 的用户离场理由 |
| 连续天数、习惯强度评分 | 形态是**清单**不是习惯，完成率本身就是记录，不需要另算 | Day 3 定位决策 |
| 多视图切换（日历 / 看板 / 时间线 / 四象限） | 主屏不能有第二层 —— 打开即达优先于功能丰富 | 学 Streaks，不学滴答清单 |
| 云同步 / 多设备 | 需要后端与账号，成本翻倍；单机够用 | 单人自用 |
| 账号与登录 | 同上，且登录会挡在"打开就打勾"前面 | 10 秒判据 |
| 手机 App / 小程序 | 应用商店注册、类目、审核流程重，网页已足够 | `research.md` 第六节；本项目早先在"小程序方案"上踩过注册 / 类目 / 审核这一串坑 |
| 后台定时推送（服务号 / 邮件 / 系统通知） | 无服务端；而且提醒已整体移交手机日历，根本不需要推 | D8 |
| 番茄钟、笔记、看板、子任务、标签、优先级 | 功能过剩，抬高打开成本 | 滴答清单的教训 |
| 复杂统计图表与仪表盘 | 只需要今日进度一个数字 | 10 秒判据 |
| 自然语言识别日期（"下周三 23:00"） | 好用但不是必需，MVP 用标准时间输入 | 第九节后续 |
| 外部平台自动导入（学习通 / 雨课堂 / 企业微信） | 三个都没有面向个人的官方接口 | research.md 第八节 |
| 协作、分享、排行榜 | 单人工具，社交压力会把工具变味 | 项目定义 |
| 撤销栈、编辑历史 | 过度设计 | 单人单机 |

**砍这些的代价（诚实写下来）**

1. 换手机或换浏览器，数据不跟着走
2. **提醒得自己录进手机日历** —— 本工具不管提醒了。这是 D8 的代价，也是"不麻烦"换来的：省掉的是网页那一整套折腾，多出来的是你要在日历里多建一条
3. 手机浏览器长期不访问可能把数据清掉（见第九节 R1，"添加到主屏幕"可缓解）

三条都在第九节有对应处理，但 **MVP 阶段接受它们**。

---

## 五、数据模型

存储：localStorage 单个 key `habit-checkin:v1`，值为 JSON。

**清单项 item**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 唯一标识（时间戳 + 随机） |
| `title` | string | 事项标题，非空 |
| `date` | string | 所属日期，`YYYY-MM-DD` |
| `done` | boolean | 是否完成 |
| `createdAt` | string | 创建时间 ISO |
| `doneAt` | string \| null | 完成时间 |

> **只存一个实体**。原来还有个「提醒项 reminder」（含 `eventTime` / `leadMinutes`），
> 已随提醒功能一并移除（D8）—— 时间点的事现在记在手机日历里，不进这个仓库、不进这张表。

**规则**

- 时间全部用**本地时间**，不做 UTC 转换 —— 一个人一台手机，没有时区问题需要解决
- "今天"以本地日期为准；页面开着跨过零点时，自动切换到新的一天，未勾项进入逾期区
- 数据量级：一年几百条，不需要索引、不需要分页
- 存储位置是**手机浏览器**的 localStorage；iOS Safari 对长期不访问的站点会清数据，见第九节 R1

---

## 六、页面结构（单一主屏）

**视图清单（就这些，没有第 4 个）**

| 视图 | 形态 | 什么时候出现 |
| --- | --- | --- |
| 主屏（唯一页面） | `index.html` 单页，无导航、无菜单、无跳转 | 每次打开就是这个，不经过任何中间页 |
| 逾期区 | 主屏顶部的区块，不是独立页面 | 存在昨天及更早的未勾项时才渲染；没有就整块不出现 |
| 空状态 | 主屏内的一行提示 + 示例项 | 今日清单一条都没有时 |

> 明确不做：设置页、统计页、日历视图、看板视图、二级详情页、弹窗表单（第四节已列为不做的项）。

从上到下，没有导航、没有设置页、没有二级页面：

```
┌────────────────────────────────────┐
│ 9 月 19 日 · 今日进度 3/5           │   ← 日期 + 进度
├────────────────────────────────────┤
│ 逾期（2）                           │   ← 有才显示
│  ○ 交实验报告      [挪到今天] [删]  │
├────────────────────────────────────┤
│ 今日清单                            │
│  ● 买咖啡豆                         │
│  ○ 复习第三章           [删]        │
│  + 添加一项…                        │
└────────────────────────────────────┘
```

**空状态**：今日为空时显示一行提示与示例项，不显示空白页。

---

## 七、验收标准

> 这一节是硬判据。Day 7 做完 MVP 后逐条打勾，任何一条不过就不能算完成。

### A · 功能

| 编号 | 验收项 | 判据 |
| --- | --- | --- |
| A1 | **打开成本** | 从打开网页到勾完 5 项，≤ **10 秒**（含加载） |
| A2 | 添加效率 | 添加一条清单项 ≤ 3 次操作（点输入框 → 输入 → 回车） |
| A5 | 勾选与进度 | 勾选后进度 +1；刷新页面状态保留 |
| A6 | 逾期区行为 | 昨天未勾项今天出现在逾期区、**不在**今日清单；点"挪到今天"后计入今日进度 |
| A7 | 数据持久化 | 关闭浏览器再打开，数据不丢 |

> **A3 / A4 / A8 已随提醒功能删除**（D8）。编号保留空缺，便于对照历史。

### B · 边界

| 编号 | 验收项 | 判据 |
| --- | --- | --- |
| B1 | 空标题 | 不允许添加，输入框保持原样 |
| B3 | 跨零点 | 页面开着到 00:00，今日清单自动切到新一天，未勾项进入逾期区 |
| B4 | 容量 | 单日 100 条内页面操作无明显卡顿 |

> **B2 / B5 已随提醒功能删除**（D8）。

### C · 反向验收（确认该砍的都砍了）

| 编号 | 验收项 | 判据 |
| --- | --- | --- |
| C1 | 无敏感文件 | 仓库中不存在 `.env`、密钥、数据库文件 |
| C2 | 无网络依赖 | 代码中没有任何指向外部域名的请求 |
| C3 | 无账号体系 | 没有登录 / 注册 / 头像 |

### D · 交付形态

| 编号 | 验收项 | 判据 |
| --- | --- | --- |
| D1 | **手机上能打开** | 手机浏览器打开网址即用 —— 不用装 App、不用 `npm install`、不用账号。**✅ 2026-09-19 已实测通过**（Henry 手机上打开 `https://daily-checkin-list.app.workbuddy.host/` 确认可访问） |
| D2 | 体积 | 总量控制在单文件（最多 3 个文件）内 |

> **D1 改过两次**：原来写"双击 `index.html`"，那是电脑上的动作，手机上做不到（`file://` 在手机浏览器不可用）；
> 后改为「有一个网址」。**托管方式**：计划中的 GitHub Pages 因组织权限受阻（见第十节 D10），
> 已改用 WorkBuddy 发布通道，网址就是上面那个。
> 注：页面被静态托管本身不算"外部请求"；但代码里**不许引用任何 CDN 资源**（字体、图标库），否则 C2 不成立。

---

## 八、用户故事（余力加练）

**US-1 · 早晨列清单**
> 作为**每天早起要安排一天的人**，我希望**打开网页就能看到今天要做的事并逐项勾掉**，以便**在十秒内完成打卡，不因为工具麻烦而放弃**。

**US-2 · 别错过高铁（已移交，不在本工具范围）**
> 原故事：作为**要在 14:00 赶高铁的人**，我希望**提前 90 分钟就收到提醒**，以便**留出路上时间，而不是在车开走时才被通知**。
>
> 2026-09-19 按 **D8** 移交给**手机自带日历 / 提醒 App**：这类事直接在日历里建、设好提前量，关着 App 也会响。
> 本工具不再承担，对应验收项 A4 / A8 已一并删除。

**US-3 · 昨天没做完的（本次决策项）**
> 作为**昨天有件事没做完的人**，我希望**它今天出现在顶部的逾期区、并且不在今日清单里**，以便**今天的清单保持干净，同时这件事不会被悄悄忘掉**。
>
> 验收：打开网页 → 逾期区显示该项 → 点「挪到今天」→ 它进入今日清单且计入进度。（对应 A6）

---

## 九、风险与后续

| 风险 | 影响 | MVP 的处理 | 后续方向（本期不做） |
| --- | --- | --- | --- |
| R1 **手机浏览器会清掉长期不访问的站点数据**（iOS Safari 约 7 天无交互） | 数据悄悄没了 | **建议「添加到主屏幕」**—— 从主屏幕打开的站点有独立存储，可豁免这类清理 | JSON 备份（触发条件见下表） |
| R2 手动清缓存 / 用无痕模式 | 记录全没 | 接受；页面里提示一句"别用无痕模式" | 导出 / 导入 JSON 备份 |
| R3 只能一台设备用 | 换手机不方便 | 接受 | 云同步（需后端） |
| R4 手动录清单嫌麻烦 | 可能不用了 | 手动录入 | 外部平台导入（research 第八节） |
| R5 **提醒不在本工具里了** | 如果日历里也没录，就真没人提醒你 | 接受 —— 这是 D8 的代价，换来的是不再折腾网页通知 | 无（D8 已定，不再回头） |

> R1 那条要 Day 7 在真机上确认一次：连着用一个星期，看数据到底会不会被清。
> 如果被清了，"添加到主屏幕"也没用，那就得提前做 JSON 备份。

**后续候选 —— 每条都必须写清「什么条件下才做」**，写不清就不许留在文档里（否则就是一句空话）

| 候选 | 什么条件下才动手 |
| --- | --- |
| JSON 备份与恢复 | 真的丢过一次数据（对应 R2），或换电脑时发现手工重录太痛 |
| 完成率统计 | "今日进度"这一个数字不够用了（比如开始想回顾"这周完成了几成"） |
| 外部平台消息导入 | `research.md` 第八节那三个平台开放了面向个人的接口（目前三个都没有） |

**以上三条当前一律不做**；触发条件没出现就不动。（原「自然语言识别日期」是为提醒服务的，D8 之后没有场景了，删掉。）

---

## 十、决策记录

| 编号 | 决策 | 结论 | 出处 |
| --- | --- | --- | --- |
| D1 | 形态 | 清单（不是习惯打卡） | Day 3 |
| D2 | 提醒 | ~~分截止型与出发型~~ **已作废** —— 提醒整体移交手机日历 | Day 3 → **D8 撤销** |
| D3 | 未完成项 | 进逾期区，不自动顺延、不归档 | **Day 4** |
| D4 | 存储 | localStorage，无后端 | Day 3 |
| D5 | 页面 | 单一主屏，无导航无二级页 | Day 3（学 Streaks） |
| D6 | 提醒触发 | ~~仅页面打开时高亮~~ **已作废** | Day 4 → **D8 撤销** |
| D7 | 关着浏览器的提醒 | ~~导出 `.ics` 交给系统日历~~ **已作废** | Day 4 追加 → **D8 撤销** |
| D8 | **提醒归谁** | **手机自带日历 / 提醒 App**；网页不做提醒，只做打卡清单 | Day 4（2026-09-19 拍板） |
| D9 | 部署 | **要有一个手机能打开的网址**（这是 D1 的前提）；~~GitHub Pages 首选~~ **实际方案见 D10** | Day 4 |
| D10 | **托管方式** | **WorkBuddy 发布通道** —— `https://daily-checkin-list.app.workbuddy.host/`；GitHub Pages 因组织权限暂不可行 | Day 4（2026-09-19 实测） |

**D8 是怎么定的**（三条路都走过一遍，记下来免得以后重新纠结）

| 候选 | 结论 | 理由 |
| --- | --- | --- |
| 网页提醒 + 导出 `.ics` | ❌ 撤销（D7 曾采纳） | 手机上每次新增 / 改提醒都要重新导出、导入一次 —— **"太麻烦"正是你要避免的** |
| 网页通知（PWA / Notification API） | ❌ 否 | iOS 必须先「添加到主屏幕」、Android 也要授权，且网页在后台就不可靠；而手机日历本来就会响 |
| 定时推送微信 / 邮件 | ❌ 否 | 需要后端存储 + 常驻服务，等于推翻 D4「无后端」 |
| **手机自带日历 / 提醒 App** | ✅ 采用 | 原生支持提前量、关着 App 也响、零维护 —— 在手机上它本来就是最优解 |

**D10 · 为什么最后没走 GitHub Pages**（2026-09-19 实测结论）

| 事实 | 说明 |
| --- | --- |
| 仓库属于**组织**，不是个人账号 | `habit-checkin` 挂在 `XingHo-VibeCoding`（Organization）名下 |
| 当前账号只有**写权限** | 能 `git push`，但 Settings 页面提示"You don't have access to repository options" —— 开 Pages 需要 admin，成员身份不够 |

三条出路与取舍：

| 候选 | 结论 | 理由 |
| --- | --- | --- |
| 请组织管理员开 Pages 或给 admin | ❌ 放弃 | 要等别人，不可控 |
| Fork 到个人账号再开 Pages | 🕐 延后 | 可行且网址归自己，但要多点两次、以后维护两个仓库 —— 对一个自用小工具不划算 |
| **WorkBuddy 发布通道** | ✅ 采用 | 零账号、零点击，1 分钟出网址；代价是**改完要手动重新发布一次**（不自动同步） |

**发布时的两个约束（已落到做法里）**

1. 发布目录是 `.deploy/`，**不是项目根目录** —— 根目录里有 `PRD.md`、`research.md`、`AGENTS.md` 和工作日志，全量发布会把它们变成公网可读文件。`.deploy/` 只放网页文件，已加进 git 忽略。
2. 已验证这些文档在站点上均返回 404，暴露面没有问题。

---

## 十一、AI 自检

按 Day 4 要求对本文档做一次自检，逐项对照验收标准。

### 自检方法

1. **内部一致性**：第七节的每条验收项，是否都能在第三、五节找到对应的功能或字段定义
2. **可验证性**：每条判据是否有明确数字或可观察结果，而不是"好用""流畅"这类形容词
3. **范围纪律**：第三节列出的功能，是否有任何一条实质属于第四节"不做"清单
4. **反向检查**：第四节的每条"不做"，是否与Day 3 研究结论或项目定义冲突

### 自检结果

**必须修改级：无。**

逐条对照结论：

| 检查 | 结果 |
| --- | --- |
| A1 ↔ F1.2 / F1.3 | ✅ 有功能支撑，判据带数字（10 秒、5 项） |
| A2 ↔ F1.4 | ✅ ≤3 次操作可数 |
| A5 / A7 ↔ F1.2 / F4.1 | ✅ 状态与持久化都有落点 |
| A6 ↔ F3 | ✅ 与 D3 决策一致，US-3 也对齐 |
| C2 ↔ 无网络依赖 | ✅ 与 D4 决策自洽；纯静态页面，不发请求 |
| 范围纪律 | ✅ 第三节 3 组功能均未越界到第四节 —— 提醒已明确列入"不做"（D8） |

**建议级（不影响本次交付，Day 7 实测时处理）**

1. **A1 无法在 PRD 阶段自证** —— 10 秒是主观计时，Day 7 应实测三次取中位数，写进当天记录。
2. **B3 跨零点在页面休眠时可能不触发** —— 浏览器会把后台标签页的定时器降频。建议 Day 7 实现时不用定时器，改为**每次渲染时比较"存储的日期"与"当前日期"**，不一致就切天。这样即使页面挂了一夜，回到前台也会正确切换。
3. **页面上要不要写一句"提醒请用手机日历"** —— 建议加一行小字，避免以后自己纳闷"这工具怎么不提醒了"。属于体验优化，Day 7 顺手加。
4. **B4 的 100 条是拍的数字** —— 真实使用一天不会超过 20 条，这条可以放宽或删除。

**自检结论**：文档通过。四条建议均为优化项，不构成"必须修改"。

### 追加复核（2026-09-19）

**第一次范围变更**：追加 F6（`.ics` 导出）与 D7 —— 后来被 D8 撤销。

**第二次范围变更（当前生效）**：提醒整体移交手机日历，砍掉 F2 / F5 / F6，前提从"Windows 单机"改为"手机为主"。重新自检：

| 检查 | 结果 |
| --- | --- |
| 文档里还有没有残留的提醒内容 | ✅ 已清 —— 第三节、第五节（reminder 实体）、第六节（提醒区块）、第七节（A3/A4/A8、B2/B5）、第八节（US-2）都已处理 |
| D1 与第二节是否自洽 | ✅ 是 —— 手机为主 → 必须有网址 → D1 改成部署后可打开，**且 2026-09-19 已在手机上实测通过** |
| C2 无外部请求是否仍成立 | ✅ 成立 —— 纯静态页面托管，代码不发请求、不引 CDN |
| 数据模型是否与范围一致 | ✅ 是 —— 只剩 item 一个实体 |
| 有没有留下"孤儿引用" | ✅ 已逐处标注 —— D2 / D6 / D7 标为作废，A3/A4/A8、B2/B5 标为已删除并保留编号空缺 |

**新增两条待验**（Day 7 必须实测，不能只看代码）

1. **R1**：手机浏览器会不会清掉数据 —— 连着用一周看；若被清，"添加到主屏幕"是否管用。
2. **A1 的 10 秒**：网址本身已验通（D1 通过），但「打开到勾完 5 项 ≤ 10 秒」要在手机上重测 —— 手机上打字比电脑慢，10 秒可能保不住，得实测。
3. **重新发布机制**：Day 7 改完功能要手动重新发布一次，容易忘 —— 建议写进当天的收尾清单。

### 功能拆解检查（Day 4 实战步骤 2）

逐个功能问一句：**它能不能在一天内被拆成可完成的小任务？**

| 功能 | 拆出来的小任务（按该做的顺序） | 一天内做得完吗 |
| --- | --- | --- |
| F1 今日清单 | 渲染列表 → 勾选切换 → 进度数字 → 添加（回车） → 删除 | ✅ 一个下午 |
| F3 逾期区 | 按日期筛未完成项 → 渲染顶部区块 → 「挪到今天」→「删除」→ 空则整块隐藏 | ✅ 半天 |
| F4 数据存储 | localStorage 读写 → 首次示例项 → 一键清空 | ✅ 一小时 |
| 部署（D1 要求） | 同步网页文件到 `.deploy/` → 重新发布 → 手机打开网址 → 添加到主屏幕 | ✅ 已做（实测通过） |

**拆解结论**：砍掉提醒之后，MVP 只剩三组功能，**Day 7 一天做完绰绰有余** —— 空出来的时间正好用来做真机验证（R1 的数据会不会被清、A1 的 10 秒在手机上还成不成立）。

**完成后检查预演**（随机挑一个功能，回答「谁、在什么页面、做什么、看到什么」）

以 F3 逾期区为例：

| 问题 | 回答 |
| --- | --- |
| 谁 | Henry 本人，手机上使用 |
| 在什么页面 | 唯一主屏顶部的「逾期」区块（没有第二个页面） |
| 做什么 | 对昨天没勾完的那条，点「挪到今天」 |
| 看到什么 | 该条从逾期区移到今日清单，顶部进度分母 +1（3/5 → 3/6）；逾期区空了就整块消失 |
