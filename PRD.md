# PRD：每日打卡清单（habit-checkin）· MVP v0.1

| 项 | 内容 |
| --- | --- |
| 项目 | habit-checkin（每日打卡清单） |
| 版本 | v0.1 · Day 4（第 1 周）；**2026-09-21 按 D11 恢复提醒功能** |
| 范围 | Day 7 之前能做完的 MVP |
| 状态 | 待评审 |
| 上游文档 | `research.md`（Day 3 需求研究） |
| 技术选型 | Day 5 决定，本文档不涉及 |

---

## 一、背景与目标

**一句话**：给 Henry 自己用的一个**手机网页清单** —— 打开就能看到今天要做的事、做完勾掉；同时对作业截止、赶火车这类**有明确时间点的事**，在**该开始行动的时刻**提醒。

**目标**

| 编号 | 目标 | 怎么算达成 |
| --- | --- | --- |
| G1 | 每天早晨能快速列完今天要做的事并逐项勾掉 | 打开到勾完 ≤ 10 秒（第七节 A1） |
| G2 | 不会错过有明确时间点的事 | 打开着：提醒按"该行动的时刻"排序并置顶（A3 / A4）；关着：导出 `.ics` 导入系统日历后照响（A8） |

**非目标**（本项目不解决）：团队协作、习惯养成激励、跨设备同步、数据分析。

> **提醒功能经历了一次推翻又恢复**（别再反复，记在这里）：
>
> - **2026-09-19 上午**（D8）：考虑到"主要在手机上用、不要太麻烦"，把提醒整块移交给手机日历，砍掉 F2 / F5 / F6。
> - **2026-09-21**（**D11，当前生效**）：实际一想发现真正的痛点不是操作麻烦，而是**割裂** ——
>   在网页里写下「交作业」，提醒却在日历里，两处各记各的，打开清单看不见今天有哪些卡时间点的事，还容易漏。
>   所以把提醒搬回网页：**恢复 F2（提醒列表）/ F5（提醒边界）/ F6（导出 `.ics`）**。
> - **代价说在前面**：搬回网页**不等于关着也响** —— 数据存在浏览器里，关了外面读不到，这条物理限制没变。
>   "关着也能提前响"仍然只能靠 F6 导出 `.ics`、导入系统日历。所以买的是"网页里能看见"，
>   付的是**每次新增或修改提醒要多一轮导出导入**（iOS Safari 下载 `.ics` 会直接弹「加入日历」，约 10 秒）。

---

## 二、用户与使用场景

**用户**：一个人 —— Henry 本人。**主要在手机上用**（手机浏览器打开，建议「添加到主屏幕」），电脑为辅。

> **这条改过**：原写的是"Windows，浏览器打开"。2026-09-19 你说主要用手机，前提就变了 ——
> 手机浏览器打不开本地 `file://` 文件，所以本工具**必须有一个网址**（见第七节 D1；托管方式见第十节 D10）。

| 场景 | 时间 | 动作 | 关键要求 |
| --- | --- | --- | --- |
| A · 早晨列清单 | 起床后 | 手机打开网页 → 看今天要做什么 → 勾掉已做的 → 补两条新的 | 一步到位，不能要我先选视图或先登录 |
| B · 白天被提醒 | 随时 | 打开网页 → 看见"该出门了 / 快到截止了"；**关着的时候**由系统日历响（需先导出导入，见 F6） | 最近的一件事必须在第一屏，不用找 |
| C · 晚上收尾 | 睡前 | 扫一眼还剩什么 → 把昨天没做完的挪到明天或删掉 | 未完成的事不能悄悄消失 |

**约束**：每天至少打开一次；**打开成本决定成败** —— 行为自动化中位数是 66 天（Harkin 等，138 项研究），每天多花 20 秒，累计起来就是放弃的理由。这条是所有取舍的最高判据。

---

## 三、功能范围（MVP 做什么）

> **F2 / F5 / F6 于 2026-09-21 恢复**（决策 D11）—— 它们曾在 2026-09-19 被 D8 整块砍掉，
> 那段历史保留在第十节，不删。MVP 现在是五组：**F1 今日清单 / F2 提醒事项 / F3 逾期区 / F4 数据存储 / F6 日历导出**
> （F5 不是独立功能，是提醒的边界说明）。

### F1 · 今日清单

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| F1.1 | 列表展示 | 今天要做的事，一件事一行 |
| F1.2 | 勾选 / 取消 | 点一下圆点切换完成状态 |
| F1.3 | 今日进度 | 顶部显示 `3/5`，只统计**今天**的项 |
| F1.4 | 添加 | 输入框输入标题，回车即加入今日 |
| F1.5 | 删除 | 每行右侧删除按钮 |

**F1 用户动作 → 可观察结果**

| 用户动作 | 可观察结果（眼睛能直接看到的） |
| --- | --- |
| 点一下某行左侧的空心圆 | 圆点变实心、标题划掉；顶部进度分子 +1（3/5 → 4/5） |
| 再点一次实心圆 | 回到空心，标题恢复，进度分子 −1 |
| 在输入框打字后按回车 | 新项出现在今日清单末尾；输入框清空且保持聚焦，可继续输入下一条 |
| 点某行右侧删除按钮 | 该行立即消失，进度分母 −1（3/5 → 3/4） |
| 什么都不输直接回车 | 没有任何反应，输入框保持原样（对应 B1） |

### F2 · 提醒事项（D11 恢复）

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| F2.1 | 添加 | 标题 + 事件时间 + 提前量（分钟，默认 0） |
| F2.2 | 排序 | 按**提醒时刻**升序，最近的置顶 |
| F2.3 | 临近高亮 | 当前时间 ≥ 提醒时刻 → 该条高亮 |
| F2.4 | 已过期标记 | 事件时间已过 → 标红显示"已过期" |
| F2.5 | 完成 | 可标记完成，完成后退出高亮 |

**F2 用户动作 → 可观察结果**

| 用户动作 | 可观察结果（眼睛能直接看到的） |
| --- | --- |
| 填「标题 + 事件时间 + 提前量」后提交 | 该条按**提醒时刻**插入列表中的正确位置，不是堆在末尾 |
| 等到提醒时刻（now ≥ remindAt） | 该条自动高亮并升到提醒区最顶部 —— 不需要刷新页面也能变 |
| 事件时间已过 | 该条标红显示"已过期"，不再进入高亮（对应 B2） |
| 勾选一条提醒为完成 | 高亮撤销，该条排到提醒列表末尾 |
| 设 14:00 的高铁、提前 90 分钟 | 到 10:30 时这条进入高亮，而不是 14:00（对应 A4） |

**提前量的含义**：提醒时刻 = 事件时刻 − 提前量。作业设 0（到点提醒），赶火车设 90（提前一个半小时喊你出门）。这是 Day 3 研究里由"赶火车"这个例子逼出来的区分 —— 少了它，工具会在火车开走时才提醒你。

### F3 · 逾期区（今日决策）

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| F3.1 | 集中展示 | 昨天及更早**未完成**的清单项，堆在页面顶部「逾期」区 |
| F3.2 | 两个动作 | 挪到今天 / 删除，只有这两种 |
| F3.3 | 不计入进度 | 逾期项不算进今日 `n/m` 的分母，避免进度数字虚高 |
| F3.4 | 无则不显示 | 没有逾期项时该区块完全隐藏 |

**F3 用户动作 → 可观察结果**

| 用户动作 | 可观察结果（眼睛能直接看到的） |
| --- | --- |
| 第二天打开页面（昨天有没勾的项） | 顶部出现「逾期(n)」区块；该项**不在**今日清单里，也不进今日进度分母 |
| 点逾期项右侧「挪到今天」 | 该条从逾期区移到今日清单，进度分母 +1（3/5 → 3/6） |
| 点逾期项右侧「删除」 | 该条彻底消失，不进今日清单、也不再出现在逾期区 |
| 把逾期区最后一条处理掉 | 整个逾期区块隐藏，页面顶部直接回到日期 + 进度那一行 |

> **为什么是逾期区而不是自动顺延**，分两半说清楚，一半有据、一半是推断：
>
> - **有据的一半**：自动处理会把事弄丢。滴答清单用户反馈"每日重复任务当天没完成，第二天就不延续了"（`research.md` 第四节③）—— 事就这么没了，这是最糟的结果。
> - **推断的一半**：自动顺延会让清单越积越长、几十条未完成堆在一起让人不想打开。**这条是我们的推断，`research.md` 里没有直接证据**，Day 7 用起来觉得不对可以回头改。
>
> 两半合起来才推出逾期区：保留"今日清单每天是干净的"（学 Streaks 的打开即达），同时保证没有事被悄悄丢掉。

### F4 · 数据存储

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| F4.1 | 本地持久化 | 浏览器 localStorage，刷新、关闭浏览器都不丢 |
| F4.2 | 首次引导 | 首次打开给 3 条示例项，可一键清空 |

**F4 用户动作 → 可观察结果**

| 用户动作 | 可观察结果（眼睛能直接看到的） |
| --- | --- |
| 勾几项后按 F5 刷新 | 勾选状态、进度数字、清单内容全部保持原样 |
| 关掉浏览器第二天再打开 | 昨天的数据还在（对应 A7） |
| 第一次打开这个网页 | 看到 3 条示例项，旁边有「清空示例」 |

### F5 · 提醒触发的边界（重要，写在明处）· D11 恢复

**有两条提醒通路**：

| 通路 | 什么时候能收到 | 要不要手动操作 |
| --- | --- | --- |
| 1 · 页面内高亮 | 网页打开着的时候 | 不用，打开就看见 |
| 2 · 系统日历（导出 `.ics`，见 F6） | **关着浏览器也能收到** | 每次新增 / 改提醒后要导入一次 |

不做后台推送 —— 没有服务端，网页不会主动发东西给你。要"关着也能收到"，靠的是把提醒**交给系统日历**（F6），而不是靠网页自己弹。

**F5 用户动作 → 可观察结果**（这条是边界，把话说死）

| 用户动作 | 可观察结果（眼睛能直接看到的） |
| --- | --- |
| 网页关着，到了提醒时刻 | **不会弹窗** —— 没有任何提示（除非你导出过日历，见 F6） |
| 之后重新打开页面 | 该条已经是高亮状态（若事件时间也已过，则显示"已过期"） |

### F6 · 日历导出（.ics）· D11 恢复

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| F6.1 | 一键导出 | 把**未完成、且事件时间还没到**的提醒导出成一个 `.ics` 文件 |
| F6.2 | 提前量变闹钟 | 每个事件挂一个闹钟，响的时刻 = 事件时刻 − `leadMinutes`，由**系统日历**来响 |
| F6.3 | 稳定的 UID | 用 `reminder.id` 当 UID —— 重复导入是更新，不会堆出重复事件 |
| F6.4 | 已完成的导出 | `done === true` 的不进文件，避免日历里堆垃圾 |

**导出范围为什么只挑「未完成且未过期」**：已经过去的事件放进日历只会变成一条历史记录、根本不会响；已经处理完的也一样。这两类留在网页里足够，不需要占用日历。

**F6 用户动作 → 可观察结果**

| 用户动作 | 可观察结果（眼睛能直接看到的） |
| --- | --- |
| 点「导出日历」 | 浏览器下载一个 `habit-checkin-reminders.ics` 文件 |
| 在手机上打开这个文件 | **iOS**：Safari 下载后直接弹「加入日历」，点一下即可；**安卓**：用日历 App 打开导入 |
| 到了该行动的时刻 | **网页可以是关着的**，系统日历照样弹出提醒（对应 A8） |
| 改了提醒内容后重新导出再导入 | 日历里那一条被更新，不会变成两条（对应 B5） |

**三个已经查证过的坑**

1. **导入的是快照，不是订阅** —— 文件进去之后就跟网页断开了，网页里改提醒，日历不会跟着变。
   能自动更新的那种叫「日历订阅」，需要一个公网上的文件地址，我们没有服务端，做不到。
2. **Windows 11 自带的「日历」应用已在 2024-12-31 停服**，现在得进新版 Outlook；而新版 Outlook
   部分版本**双击 `.ics` 打不开**，必须从日历界面里手动「从文件上传」。所以别指望双击一定好使。
   （好消息：**手机上反而更顺** —— iOS 点一下就加入日历，这是 2026-09-21 重新评估时确认的关键事实。）
3. **每次改提醒都要重走一遍** —— 这是选这条路绕不开的代价，D11 拍板时已确认接受。
   UID 稳定，所以重复导入是**更新**而不是变两条（见 F6.3 / B5）。

---

## 四、明确不做（砍掉了什么，为什么砍）

| 砍掉的功能 | 为什么砍 | 依据 |
| --- | --- | --- |
| 网页通知弹窗（Notification API / PWA 推送） | iOS 必须先「添加到主屏幕」、Android 也要授权，且网页在后台时不可靠 —— 关着要能响改用导出 `.ics`（F6） | **D11**（2026-09-21） |
| 游戏化（积分 / 等级 / 成就 / 宠物） | 激励层会反客为主，变成第二个要维护的系统 | Habitica 的用户离场理由 |
| 连续天数、习惯强度评分 | 形态是**清单**不是习惯，完成率本身就是记录，不需要另算 | Day 3 定位决策 |
| 多视图切换（日历 / 看板 / 时间线 / 四象限） | 主屏不能有第二层 —— 打开即达优先于功能丰富 | 学 Streaks，不学滴答清单 |
| 云同步 / 多设备 | 需要后端与账号，成本翻倍；单机够用 | 单人自用 |
| 账号与登录 | 同上，且登录会挡在"打开就打勾"前面 | 10 秒判据 |
| 手机 App / 小程序 | 应用商店注册、类目、审核流程重，网页已足够 | `research.md` 第六节；本项目早先在"小程序方案"上踩过注册 / 类目 / 审核这一串坑 |
| 后台定时推送（服务号 / 邮件 / 系统通知） | 无服务端；关着浏览器要能收到，改由**导出 `.ics` 交给系统日历**承担（F6）—— 那是用户导入的文件，不是我们推的 | D7 / D11 |
| 番茄钟、笔记、看板、子任务、标签、优先级 | 功能过剩，抬高打开成本 | 滴答清单的教训 |
| 复杂统计图表与仪表盘 | 只需要今日进度一个数字 | 10 秒判据 |
| 自然语言识别日期（"下周三 23:00"） | 好用但不是必需，MVP 用标准时间输入 | 第九节后续 |
| 外部平台自动导入（学习通 / 雨课堂 / 企业微信） | 三个都没有面向个人的官方接口 | research.md 第八节 |
| 协作、分享、排行榜 | 单人工具，社交压力会把工具变味 | 项目定义 |
| 撤销栈、编辑历史 | 过度设计 | 单人单机 |

**砍这些的代价（诚实写下来）**

1. 换手机或换浏览器，数据不跟着走
2. **关着浏览器要收到提醒，得自己导出导入一次 `.ics`** —— 网页不会主动推送你。这是 F6 绕不开的代价：
   买的是"网页里能看见今天的时间点安排"，付的是每次新增或修改提醒多走一轮（手机上约 10 秒）
3. 手机浏览器长期不访问可能把数据清掉（见第九节 R1，"添加到主屏幕"可缓解）

三条都在第九节有对应处理，但 **MVP 阶段接受它们**。

---

## 五、数据模型

存储：localStorage 单个 key `habit-checkin:v1`，值为 JSON。

**清单项 item**

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 唯一标识（时间戳 + 随机） |
| `title` | string | 事项标题，非空 |
| `date` | string | 所属日期，`YYYY-MM-DD` |
| `done` | boolean | 是否完成 |
| `createdAt` | string | 创建时间 ISO |
| `doneAt` | string \| null | 完成时间 |

**提醒项 reminder**（D11 恢复）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 唯一标识 |
| `title` | string | 事件标题 |
| `eventTime` | string | 事件时刻，本地时间 ISO |
| `leadMinutes` | number | 提前量（分钟），默认 0 |
| `done` | boolean | 是否处理完 |
| `createdAt` | string | 创建时间 ISO |

**派生字段（不存，每次计算）**

```
提醒时刻 remindAt = eventTime − leadMinutes
是否临近         = now ≥ remindAt 且 eventTime > now
是否过期         = now > eventTime
```

**规则**

- 时间全部用**本地时间**，不做 UTC 转换 —— 一个人一台手机，没有时区问题需要解决
- "今天"以本地日期为准；页面开着跨过零点时，自动切换到新的一天，未勾项进入逾期区
- 数据量级：一年几百条，不需要索引、不需要分页
- 存储位置是**手机浏览器**的 localStorage；iOS Safari 对长期不访问的站点会清数据，见第九节 R1

---

## 六、页面结构（单一主屏）

**视图清单（就这些，没有第 4 个）**

| 视图 | 形态 | 什么时候出现 |
| --- | --- | --- |
| 主屏（唯一页面） | `index.html` 单页，无导航、无菜单、无跳转 | 每次打开就是这个，不经过任何中间页 |
| 逾期区 | 主屏顶部的区块，不是独立页面 | 存在昨天及更早的未勾项时才渲染；没有就整块不出现 |
| 提醒区 | 主屏底部的区块，不是独立页面 | 常驻（有提醒项时显示列表，没有时只留「添加提醒」入口与导出按钮） |
| 空状态 | 主屏内的一行提示 + 示例项 | 今日清单一条都没有时 |

> 明确不做：设置页、统计页、日历视图、看板视图、二级详情页、弹窗表单（第四节已列为不做的项）。

从上到下，没有导航、没有设置页、没有二级页面：

```
┌────────────────────────────────────┐
│ 9 月 19 日 · 今日进度 3/5           │   ← 日期 + 进度
├────────────────────────────────────┤
│ 逾期（2）                           │   ← 有才显示
│  ○ 交实验报告      [挪到今天] [删]  │
├────────────────────────────────────┤
│ 今日清单                            │
│  ● 买咖啡豆                         │
│  ○ 复习第三章           [删]        │
│  + 添加一项…                        │
├────────────────────────────────────┤
│ 提醒                                │
│  ⚠ 高铁 14:00（提前 90 分钟）       │   ← 临近，高亮
│    作业截止 23:00                    │
│  + 添加提醒…                        │
│  [导出日历 .ics]                     │
└────────────────────────────────────┘
```

**空状态**：今日为空时显示一行提示与示例项，不显示空白页。

---

## 七、验收标准

> 这一节是硬判据。Day 7 做完 MVP 后逐条打勾，任何一条不过就不能算完成。

### A · 功能

| 编号 | 验收项 | 判据 |
| --- | --- | --- |
| A1 | **打开成本** | 从打开网页到勾完 5 项，≤ **10 秒**（含加载） |
| A2 | 添加效率 | 添加一条清单项 ≤ 3 次操作（点输入框 → 输入 → 回车） |
| A3 | 提醒排序 | 按提醒时刻升序，最近的一条在最顶部 |
| A4 | 提前量生效 | 事件 14:00、提前 90 分钟 → 10:30 起该条进入临近高亮 |
| A5 | 勾选与进度 | 勾选后进度 +1；刷新页面状态保留 |
| A6 | 逾期区行为 | 昨天未勾项今天出现在逾期区、**不在**今日清单；点"挪到今天"后计入今日进度 |
| A7 | 数据持久化 | 关闭浏览器再打开，数据不丢 |
| A8 | 关着浏览器也能收到 | 导出的 `.ics` 导入系统日历后，14:00 高铁 + 提前 90 分钟 → **10:30 弹出系统提醒**；此刻网页可以是关着的。Day 7 必须真导入一次、等它真响一次才算过 |

### B · 边界

| 编号 | 验收项 | 判据 |
| --- | --- | --- |
| B1 | 空标题 | 不允许添加，输入框保持原样 |
| B2 | 事件时间已过 | 标"已过期"，不进入临近高亮 |
| B3 | 跨零点 | 页面开着到 00:00，今日清单自动切到新一天，未勾项进入逾期区 |
| B4 | 容量 | 单日 100 条内页面操作无明显卡顿 |
| B5 | 重复导入 | 同一条提醒导出两次、导入两次，日历里仍然只有一条事件（UID 不变） |

### C · 反向验收（确认该砍的都砍了）

| 编号 | 验收项 | 判据 |
| --- | --- | --- |
| C1 | 无敏感文件 | 仓库中不存在 `.env`、密钥、数据库文件 |
| C2 | 无网络依赖 | 代码中没有任何指向外部域名的请求 |
| C3 | 无账号体系 | 没有登录 / 注册 / 头像 |

### D · 交付形态

| 编号 | 验收项 | 判据 |
| --- | --- | --- |
| D1 | **手机上能打开** | 手机浏览器打开网址即用 —— 不用装 App、不用 `npm install`、不用账号。**✅ 2026-09-19 已实测通过**（Henry 手机上打开 `https://daily-checkin-list.app.workbuddy.host/` 确认可访问） |
| D2 | 体积 | 总量控制在单文件（最多 3 个文件）内 |

> **D1 改过两次**：原来写"双击 `index.html`"，那是电脑上的动作，手机上做不到（`file://` 在手机浏览器不可用）；
> 后改为「有一个网址」。**托管方式**：计划中的 GitHub Pages 因组织权限受阻（见第十节 D10），
> 已改用 WorkBuddy 发布通道，网址就是上面那个。
> 注：页面被静态托管本身不算"外部请求"；但代码里**不许引用任何 CDN 资源**（字体、图标库），否则 C2 不成立。

---

## 八、用户故事（余力加练）

**US-1 · 早晨列清单**
> 作为**每天早起要安排一天的人**，我希望**打开网页就能看到今天要做的事并逐项勾掉**，以便**在十秒内完成打卡，不因为工具麻烦而放弃**。

**US-2 · 别错过高铁**（2026-09-19 曾按 D8 移交手机日历，2026-09-21 按 **D11** 收回本工具）
> 作为**要在 14:00 赶高铁的人**，我希望**提前 90 分钟就收到提醒**，以便**留出路上时间，而不是在车开走时才被通知**。
>
> 验收：页面上提前量生效（A4）；导出 `.ics` 导入系统日历后，**网页关着也会在 10:30 收到系统提醒**（A8）。

**US-3 · 昨天没做完的（本次决策项）**
> 作为**昨天有件事没做完的人**，我希望**它今天出现在顶部的逾期区、并且不在今日清单里**，以便**今天的清单保持干净，同时这件事不会被悄悄忘掉**。
>
> 验收：打开网页 → 逾期区显示该项 → 点「挪到今天」→ 它进入今日清单且计入进度。（对应 A6）

---

## 九、风险与后续

| 风险 | 影响 | MVP 的处理 | 后续方向（本期不做） |
| --- | --- | --- | --- |
| R1 **手机浏览器会清掉长期不访问的站点数据**（iOS Safari 约 7 天无交互） | 数据悄悄没了 | **建议「添加到主屏幕」**—— 从主屏幕打开的站点有独立存储，可豁免这类清理 | JSON 备份（触发条件见下表） |
| R2 手动清缓存 / 用无痕模式 | 记录全没 | 接受；页面里提示一句"别用无痕模式" | 导出 / 导入 JSON 备份 |
| R3 只能一台设备用 | 换手机不方便 | 接受 | 云同步（需后端） |
| R4 手动录清单嫌麻烦 | 可能不用了 | 手动录入 | 外部平台导入（research 第八节） |
| R5 **关着浏览器要收到提醒，得手动导出导入一次** | 改了提醒忘了导出 → 日历里还是旧的，可能漏 | 接受 —— 这是 F6 绕不开的代价；导出按钮放在提醒区显眼位置 | 日历订阅（需要公网文件地址 + 服务端，做不到） |

> R1 那条要 Day 7 在真机上确认一次：连着用一个星期，看数据到底会不会被清。
> 如果被清了，"添加到主屏幕"也没用，那就得提前做 JSON 备份。

**后续候选 —— 每条都必须写清「什么条件下才做」**，写不清就不许留在文档里（否则就是一句空话）

| 候选 | 什么条件下才动手 |
| --- | --- |
| JSON 备份与恢复 | 真的丢过一次数据（对应 R2），或换电脑时发现手工重录太痛 |
| 完成率统计 | "今日进度"这一个数字不够用了（比如开始想回顾"这周完成了几成"） |
| 自然语言识别日期（"下周三 23:00"） | 连续一周觉得手选日期比打字还烦 —— 用满两周才有资格判断（提醒功能恢复后这条重新有了场景） |
| 外部平台消息导入 | `research.md` 第八节那三个平台开放了面向个人的接口（目前三个都没有） |

**以上四条当前一律不做**；触发条件没出现就不动。

---

## 十、决策记录

| 编号 | 决策 | 结论 | 出处 |
| --- | --- | --- | --- |
| D1 | 形态 | 清单（不是习惯打卡） | Day 3 |
| D2 | 提醒 | 分截止型与出发型，数据上加 `leadMinutes` 字段（~~曾被 D8 作废~~ → **D11 恢复有效**） | Day 3 |
| D3 | 未完成项 | 进逾期区，不自动顺延、不归档 | **Day 4** |
| D4 | 存储 | localStorage，无后端 | Day 3 |
| D5 | 页面 | 单一主屏，无导航无二级页 | Day 3（学 Streaks） |
| D6 | 提醒触发 | 页面打开时高亮（~~曾被 D8 作废~~ → **D11 恢复有效**） | Day 4 |
| D7 | 关着浏览器的提醒 | 导出 `.ics` 交给系统日历（~~曾被 D8 作废~~ → **D11 恢复有效**） | Day 4 追加 |
| D8 | ~~提醒归谁：手机自带日历~~ | **已作废** —— 网页不做提醒、只做打卡清单 | Day 4（2026-09-19）→ **D11 撤销** |
| D11 | **提醒归谁（重新拍板）** | **搬回网页** —— 恢复 F2 / F5 / F6；"关着也能响"仍靠导出 `.ics` 交给系统日历 | Day 6（2026-09-21） |
| D9 | 部署 | **要有一个手机能打开的网址**（这是 D1 的前提）；~~GitHub Pages 首选~~ **实际方案见 D10** | Day 4 |
| D10 | **托管方式** | **WorkBuddy 发布通道** —— `https://daily-checkin-list.app.workbuddy.host/`；GitHub Pages 因组织权限暂不可行 | Day 4（2026-09-19 实测） |

**D11 · 为什么推翻 D8，把提醒搬回网页**（2026-09-21）

**直接起因**：你说"手机日历会不会有点麻烦"。查下来发现**麻烦不在操作步数** —— 日历录一次就完，
反而比导出导入省。**真正的痛是割裂**：在网页里写下「交作业」，提醒却在日历里，两处各记各的，
打开清单看不见今天有哪些卡时间点的事，还容易漏（清单写了、日历忘了设）。

**关键事实（别再重复推导）**：**搬回网页 ≠ 关着也响**。数据存在浏览器里、关了外面读不到，
这条物理限制两次评估都没变。所以"关着也能提前响"仍只能靠导出 `.ics` + 导入系统日历。

三条路重新过一遍：

| 候选 | 结论 | 理由 |
| --- | --- | --- |
| **网页管提醒 + 导出 `.ics`** | ✅ **采用（D11）** | 网页里能看见今天的时间点安排（解决割裂）；关着也能响（导出后由系统日历响）。代价是每次新增 / 改提醒要多一轮导出导入 —— 手机上约 10 秒（iOS Safari 下载 `.ics` 直接弹「加入日历」） |
| 只用手机日历、网页不管提醒 | ❌ 撤销（即原 D8） | 步数是省了，但两处割裂、看不见、容易漏 —— 这才是真麻烦 |
| 网页通知（PWA / Notification API） | ❌ 否 | iOS 必须先「添加到主屏幕」、Android 也要授权，后台不可靠；且要引入 Service Worker，破坏 D2 单文件 |
| 定时推送微信 / 邮件 | ❌ 否 | 需要后端存储 + 常驻服务，等于推翻 D4「无后端」 |

> **纪律**：提醒这块在 Day 4 一天内改过两次、Day 6 又改一次，**到此收敛**。
> 再要改，先说清楚新出现的事实是什么 —— 不能因为"感觉麻烦"就翻案。

**D10 · 为什么最后没走 GitHub Pages**（2026-09-19 实测结论）

| 事实 | 说明 |
| --- | --- |
| 仓库属于**组织**，不是个人账号 | `habit-checkin` 挂在 `XingHo-VibeCoding`（Organization）名下 |
| 当前账号只有**写权限** | 能 `git push`，但 Settings 页面提示"You don't have access to repository options" —— 开 Pages 需要 admin，成员身份不够 |

三条出路与取舍：

| 候选 | 结论 | 理由 |
| --- | --- | --- |
| 请组织管理员开 Pages 或给 admin | ❌ 放弃 | 要等别人，不可控 |
| Fork 到个人账号再开 Pages | 🕐 延后 | 可行且网址归自己，但要多点两次、以后维护两个仓库 —— 对一个自用小工具不划算 |
| **WorkBuddy 发布通道** | ✅ 采用 | 零账号、零点击，1 分钟出网址；代价是**改完要手动重新发布一次**（不自动同步） |

**发布时的两个约束（已落到做法里）**

1. 发布目录是 `.deploy/`，**不是项目根目录** —— 根目录里有 `PRD.md`、`research.md`、`AGENTS.md` 和工作日志，全量发布会把它们变成公网可读文件。`.deploy/` 只放网页文件，已加进 git 忽略。
2. 已验证这些文档在站点上均返回 404，暴露面没有问题。

---

## 十一、AI 自检

按 Day 4 要求对本文档做一次自检，逐项对照验收标准。

### 自检方法

1. **内部一致性**：第七节的每条验收项，是否都能在第三、五节找到对应的功能或字段定义
2. **可验证性**：每条判据是否有明确数字或可观察结果，而不是"好用""流畅"这类形容词
3. **范围纪律**：第三节列出的功能，是否有任何一条实质属于第四节"不做"清单
4. **反向检查**：第四节的每条"不做"，是否与Day 3 研究结论或项目定义冲突

### 自检结果

**必须修改级：无。**

逐条对照结论：

| 检查 | 结果 |
| --- | --- |
| A1 ↔ F1.2 / F1.3 | ✅ 有功能支撑，判据带数字（10 秒、5 项） |
| A2 ↔ F1.4 | ✅ ≤3 次操作可数 |
| A3 / A4 ↔ F2.2 / F2.3 + 派生公式 | ✅ 提前量算法在第五节写死，A4 给出具体数值可验（14:00 / 90 分钟 → 10:30） |
| A5 / A7 ↔ F1.2 / F4.1 | ✅ 状态与持久化都有落点 |
| A6 ↔ F3 | ✅ 与 D3 决策一致，US-3 也对齐 |
| A8 ↔ F6 + 第五节 `leadMinutes` | ✅ 提前量转成日历闹钟，判据带具体时刻；且注明"必须真导入一次、等它真响"才算过 |
| B2 / B5 ↔ F2.4 / F6.3 | ✅ 过期标记与 UID 复用都有落点 |
| C2 ↔ 无网络依赖 | ✅ 与 D4 决策自洽；`.ics` 是本地生成并下载的文件，代码不发请求 |
| 范围纪律 | ✅ 第三节 5 组功能均未越界到第四节 —— F6 是导出本地文件，不是后台推送 |

**建议级（不影响本次交付，Day 7 实测时处理）**

1. **A1 无法在 PRD 阶段自证** —— 10 秒是主观计时，Day 7 应实测三次取中位数，写进当天记录。
2. **B3 跨零点在页面休眠时可能不触发** —— 浏览器会把后台标签页的定时器降频。建议 Day 7 实现时不用定时器，改为**每次渲染时比较"存储的日期"与"当前日期"**，不一致就切天。这样即使页面挂了一夜，回到前台也会正确切换。
3. **导出按钮旁要写清"改了提醒后记得重新导出导入一次"** —— 否则会以为导出一次就永远自动同步了（事实不会，见 F6 坑 1）。Day 7 顺手加。
4. **B4 的 100 条是拍的数字** —— 真实使用一天不会超过 20 条，这条可以放宽或删除。
5. **提醒区放在页面底部，可能掉出首屏** —— 场景 B 要求"最近的一件事必须在第一屏"。
   提醒项通常只有 1–3 条、今日清单也不长，多半没问题；Day 7 实测若发现要滚动才看得见，
   就把提醒区上移到逾期区之上（纯布局调整，不影响功能）。

**自检结论**：文档通过。四条建议均为优化项，不构成"必须修改"。

### 追加复核（2026-09-19）

**第一次范围变更**：追加 F6（`.ics` 导出）与 D7 —— 后来被 D8 撤销。

**第二次范围变更**：提醒整体移交手机日历，砍掉 F2 / F5 / F6，前提从"Windows 单机"改为"手机为主"。

**第三次范围变更（当前生效，D11）**：提醒搬回网页，恢复 F2 / F5 / F6。重新自检：

| 检查 | 结果 |
| --- | --- |
| F6 是否越界进第四节"不做" | ✅ 否 —— 导出的是本地文件；后台定时推送仍在不做清单里 |
| D1 / D2 是否被破坏 | ✅ 否 —— 仍是单文件静态页，F6 走浏览器下载，不引入服务、不引 CDN |
| C2 无外部请求是否仍成立 | ✅ 成立 —— 代码里没有任何请求外部域名的行为 |
| 数据模型是否同步 | ✅ 是 —— `reminder` 实体已恢复，`remindAt` 派生公式写回第五节 |
| 有没有留下"孤儿引用" | ✅ 已逐处标注 —— D8 标为作废（留痕不删），D2 / D6 / D7 标为恢复有效 |
| A3 / A4 / A8、B2 / B5 是否有功能支撑 | ✅ 有 —— 对应 F2 与 F6，且 A8 注明必须真机验证 |

**新增一条待验**：A8 依赖"系统日历会按 `VALARM` 响"这个前提，Day 7 必须真的导入一次、等一次提醒，
确认弹出来了才算过 —— 只看文件生成成功不算。

（下面是**第二次变更当时**的自检记录，留作历史 —— 其中"只剩 item 一个实体""提醒已清"等结论已被 D11 推翻：）

| 检查 | 结果 |
| --- | --- |
| 文档里还有没有残留的提醒内容 | ✅ 已清 —— 第三节、第五节（reminder 实体）、第六节（提醒区块）、第七节（A3/A4/A8、B2/B5）、第八节（US-2）都已处理 |
| D1 与第二节是否自洽 | ✅ 是 —— 手机为主 → 必须有网址 → D1 改成部署后可打开，**且 2026-09-19 已在手机上实测通过** |
| C2 无外部请求是否仍成立 | ✅ 成立 —— 纯静态页面托管，代码不发请求、不引 CDN |
| 数据模型是否与范围一致 | ✅ 是 —— 只剩 item 一个实体 |
| 有没有留下"孤儿引用" | ✅ 已逐处标注 —— D2 / D6 / D7 标为作废，A3/A4/A8、B2/B5 标为已删除并保留编号空缺 |

**新增两条待验**（Day 7 必须实测，不能只看代码）

1. **R1**：手机浏览器会不会清掉数据 —— 连着用一周看；若被清，"添加到主屏幕"是否管用。
2. **A1 的 10 秒**：网址本身已验通（D1 通过），但「打开到勾完 5 项 ≤ 10 秒」要在手机上重测 —— 手机上打字比电脑慢，10 秒可能保不住，得实测。
3. **重新发布机制**：Day 7 改完功能要手动重新发布一次，容易忘 —— 建议写进当天的收尾清单。

### 功能拆解检查（Day 4 实战步骤 2）

逐个功能问一句：**它能不能在一天内被拆成可完成的小任务？**

| 功能 | 拆出来的小任务（按该做的顺序） | 一天内做得完吗 |
| --- | --- | --- |
| F1 今日清单 | 渲染列表 → 勾选切换 → 进度数字 → 添加（回车） → 删除 | ✅ 一个下午 |
| F2 提醒事项 | ① 添加表单（时间 + 提前量） → ② 按提醒时刻排序 → ③ 临近高亮 → ④ 过期标记 → ⑤ 完成态 | ✅ 但必须按 ①→⑤ 顺序做，跳着做会返工 |
| F3 逾期区 | 按日期筛未完成项 → 渲染顶部区块 → 「挪到今天」→「删除」→ 空则整块隐藏 | ✅ 半天 |
| F4 数据存储 | localStorage 读写 → 首次示例项 → 一键清空 | ✅ 一小时 |
| F6 日历导出 | 拼 `.ics` 文本 → 触发下载 → 塞闹钟（提前量） → 真机导入验证 | ✅ 半天，**但验证要另约时间**：得等到提醒真响一次 |
| F5 提醒边界 | 不是独立功能，随 F2 / F6 一起落地 | — |
| 部署（D1 要求） | 同步网页文件到 `.deploy/` → 重新发布 → 手机打开网址 → 添加到主屏幕 | ✅ 已做（实测通过） |

**拆解结论（D11 之后更新）**：单个功能都能在一天内拆完，但**F1~F6 全加一起，是一天的满量**。
所以 Day 7 建议这么排：先让 F1~F4 跑通、自己真用一天，**F6（导出）排到隔天** ——
它是唯一需要"等它真响一次"才能验收的功能，当天做当天验不完。
（这是工作量排序，不是范围砍杀：F6 仍在 MVP 范围内。）

**完成后检查预演**（随机挑一个功能，回答「谁、在什么页面、做什么、看到什么」）

以 F3 逾期区为例：

| 问题 | 回答 |
| --- | --- |
| 谁 | Henry 本人，手机上使用 |
| 在什么页面 | 唯一主屏顶部的「逾期」区块（没有第二个页面） |
| 做什么 | 对昨天没勾完的那条，点「挪到今天」 |
| 看到什么 | 该条从逾期区移到今日清单，顶部进度分母 +1（3/5 → 3/6）；逾期区空了就整块消失 |
