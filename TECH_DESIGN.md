# 技术设计：每日打卡清单（habit-checkin）· MVP v0.1

> 上游文档：`PRD.md`（功能范围与验收标准）、`research.md`（同类产品比较）
> 今日目标：用一句话说清**数据从哪来、到哪去**。
> 本文件只讲技术路线与数据流，**不含代码**（Day 5 不做写代码）。

## 0 · 一句话

> **你敲进去的每一项，只进手机浏览器自己的 localStorage，再原样读出来显示在页面上 —— 全程不出这台设备，不碰服务器。**

拆成两半，就是今天要记住的东西：

| 半句 | 含义 |
| --- | --- |
| **从哪来** | 只有一个来源：你在页面上敲的字。没有后台录入、没有外部平台同步、没有别人给你推数据 |
| **到哪去** | 只有一个去处：这台手机浏览器自带的 localStorage。不上传、不备份、不换设备同步 |

**为什么是这样**：`PRD.md` 的 D4（localStorage，无后端）和 D8（提醒交给手机日历）两条决策，根子都在这儿 —— 一旦数据要离开这台设备，就必然要配服务器、账号、数据库，那就不是"自用小工具"了。

---

## 1 · 前后端与数据库分工

先说结论：**本项目没有后端，也没有数据库。**

| 角色 | 有没有 | 在本项目里是什么 |
| --- | --- | --- |
| 前端（页面） | ✅ 有 | 你眼睛看到、手指点的那一个页面 |
| 后端（跑在服务器上的程序） | ❌ 没有 | 没有接口、没有账号系统、没有服务端逻辑 |
| 数据库 | ❌ 没有 | 数据存在浏览器自带的 localStorage 里 |
| 服务器（托管） | ⚠️ 只半个 | 只负责把网页文件送到你手机上（决策 D10），**不存数据、不跑逻辑** |

### 「托管」不等于「后端」—— 这条最容易混

Day 4 部署时拿到了网址，很容易以为"那就有服务器了"。其实服务器在这件事上的角色很小：

| 服务器做的事 | 服务器不做的事 |
| --- | --- |
| 把 `index.html` 这个文件发给你的手机 | 不知道你今天勾了几项 |
| 让手机有个网址可以打开 | 不保存你的任何一条清单 |
| （就这些） | 不在你打开页面时参与任何运算 |

**怎么自己验证**：断网后再打开那个网址，页面照样能用、勾过的数据照样在（首次打开需要联网取文件，之后浏览器会缓存）。这就是"没有后端"最直接的证据 —— 真有后端的话，断网就刷不出数据了。

### 前端的三层分工

一个网页拆开是三层，本项目三层放在同一个文件里：

| 层 | 负责 | 类比 |
| --- | --- | --- |
| HTML · 结构 | 页面上有哪些东西：标题、输入框、一行行清单 | 房子的骨架、有几个房间 |
| CSS · 样式 | 这些东西长什么样：字号、颜色、间距 | 装修和粉刷 |
| JS · 行为 | 点了会怎样：勾选、添加、删除、算进度 | 水电和开关 |

### localStorage 是什么（本项目的"数据库"）

浏览器给**每个网址单独分配**的一小块本地抽屉：

- 只在**这个网址、这个浏览器**里可见 —— 别的网站读不到，换台手机也看不到
- 关掉浏览器、关掉手机，**数据还在**（这是它能当数据库用的原因）
- **清了浏览器数据就没了**（`PRD.md` 风险 R2，MVP 阶段接受）
- 容量约 5 MB —— 装几百条清单绰绰有余（`PRD.md` 第五节：一年几百条）

---

## 2 · 技术路线

> **技术路线：单文件静态网页（原生 HTML + CSS + JS，零依赖、零构建）+ 浏览器 localStorage + 静态托管发布。**

拆成六个层面，每层一句理由、一句代价：

| 层面 | 选什么 | 一句话理由 | 代价（诚实列出） |
| --- | --- | --- | --- |
| 页面 | 原生 HTML + CSS + JS，都在 `index.html` 里 | 一个清单页用不上框架；原生写法零安装、打开就跑，符合 D1「不用 `npm install`」 | 没有现成组件，勾选动画、布局都要自己写 |
| 存储 | 浏览器 localStorage | D4 已定无后端；它能断网读写、关掉手机数据还在（A7） | 清缓存丢数据（风险 R2）、换设备不同步 |
| 第三方依赖 | **零** —— 不引任何库 | C2 要求"代码中没有任何指向外部域名的请求"：引 CDN 字体图标库会让这条不成立，断网还会缺字缺图 | 样式手写，不能白嫖现成 UI |
| 构建工具 | **无** —— 不装 node、不打包 | 改完一个文件直接发布，符合 D2「单文件」 | 用不了需要编译的语法（TypeScript、JSX 之类） |
| 文件数 | 1 个 `index.html`（上限 3 个，D2） | 单文件最省事，改哪看哪 | 文件会变长，MVP 估计 300–500 行 |
| 托管 | WorkBuddy 发布通道 | D10：组织仓库无 admin 权限，GitHub Pages 开不了；这条路零账号零点击 | 改完要**手动重新发布**一次，不自动同步 |

### 否决项：为什么不选别的（各一句）

| 候选 | 为什么不选（现阶段） |
| --- | --- |
| 前端框架（React / Vue / Svelte） | 都要构建工具和包管理，与 D2 单文件冲突；一个清单页的交互量撑不起这份复杂度 |
| UI 组件库 / CSS 框架 | 引 CDN 违反 C2，装包则要构建 —— 两头都不通 |
| 后端 + 数据库 | D4 已定无后端；数据一走网络，断网就不能用，与第 1 节的判定直接冲突 |
| 小程序 / 原生 App | Day 3 研究已否决：注册、类目、审核流程重 |
| 构建工具（Vite / Webpack） | 引入 node_modules 与构建步骤，对一个自用小工具不划算 |

### 为什么「暂时」选它（多方案比较为什么没做）

这不是"比较之后最优"，而是**约束筛完之后只剩它**：D1 手机能打开 + D2 单文件 + C2 无外部请求 + D4 无后端，四条一叠加，框架、后端、构建工具全部出局，剩下的只有原生静态页。约束不松动，对比表就没有意义 —— 所以今天采用示范路线，不做多方案比较。

**什么时候该换**：

| 触发条件 | 换成什么 |
| --- | --- |
| 单文件超过约 600 行、改一处要找半天 | 拆成 `index.html` + `style.css` + `app.js`（D2 允许最多 3 个） |
| 需要跨设备同步 | 引入后端与数据库 —— 但那推翻 D4，得回头改 PRD，不是技术选型层面能定的 |
| 交互复杂到原生写法难以维护 | 引入框架 —— 同样要先回头改 D2 |

---

## 3 · 数据流图

```text
【数据从哪来】
  你在输入框敲字 → 回车 → 多出一条清单项
  你点一下圆点   → 这一项变成已完成
  你点一下删除   → 这一项消失
        │
        ▼
【数据在哪被改】
  页面 JS 手里有一份清单（存在内存里）
  你的每个动作，都是先改这份内存里的清单
        │
        ▼  改完立刻写回
【数据到哪去】
  localStorage —— 唯一的去处
  在这台手机的浏览器里，别处没有副本
        │
        ▼
【数据怎么回来】
  下次打开页面 → 从 localStorage 读出来 → 画到屏幕上
  关掉浏览器、关掉手机再打开，数据还在（验收 A7）
        │
        ▼
【唯一的额外动作 · 跨零点】
  每次渲染都比一下「上次存的日期」和「今天」
  不一样 → 昨天没勾的项挪进顶部逾期区（验收 B3）
  不联网、不通知任何人，只是在本地挪了个位置

数据不经过的地方：
  ✗ 服务器   ✗ 网络   ✗ 别人的数据库   ✗ 云端备份   ✗ 别的设备
```

### 怎么读这张图

| 环节 | 一句话 | 对应验收 |
| --- | --- | --- |
| 从哪来 | 只有一个入口：你在页面上的操作 | A2（添加 ≤ 3 次操作） |
| 在哪改 | 先改内存里那份，改的是"当前看到的样子" | A5（勾选后进度 +1） |
| 到哪去 | 立刻写回 localStorage，这一步是**同步**的 —— 不等你关页面 | A7（关掉再开数据不丢） |
| 怎么回来 | 打开页面第一件事就是读出来、画上去 | A1（打开到勾完 ≤ 10 秒） |
| 跨零点 | **不用定时器**，每次渲染比对日期 —— PRD 自检里特意点出过这点（浏览器会降频后台标签页的定时器，定时器会漏） | B3 |

**闭环的地方要注意**：数据不是"存进去就完了"，而是**每次操作都走一遍完整闭环**（读 → 改 → 写 → 画）。所以画出来的样子和存着的东西始终一致 —— 这也是为什么刷新页面状态不会丢（A5）。

---

## 4 · 余力加练：方案变化时，哪些文件会受影响

| 改什么 | 会牵动哪些文件 | 程度 | 说明 |
| --- | --- | --- | --- |
| 页面配色、字号、间距 | `index.html` | 小 | 只动样式，数据流完全不受影响 |
| 拆成三个文件（`index.html` + `style.css` + `app.js`） | `index.html` 拆开；`PRD.md` 不用改（D2 本来就允许最多 3 个）；本文档第 2 节"文件数"那行 | 小 | **D2 已预留这条路**，真到 600 行就走它 |
| 换托管方式（WorkBuddy → GitHub Pages） | `README.md` 预览节、`PRD.md` 的 D10、`.deploy/` 目录可弃用 | 小 | 网页文件本身一个字都不用改 —— 再次说明"托管 ≠ 后端" |
| 给清单项加字段（备注、优先级之类） | `PRD.md` 第五节数据模型、`index.html` 的读写与渲染 | 中 | **要考虑旧数据**：手机里已存的项没有这个新字段，读出来得有默认值 |
| 换存储方式（不用 localStorage，改别的） | `index.html` 读写逻辑、`PRD.md` 第五节 + D4、本文档第 1/2/3 节 | 大 | 数据流整条重写 |
| 提醒功能回到网页来做 | `PRD.md` 第三节 F2/F5/F6 复活 + 第五节 reminder 实体 + 第七节 A3/A4/A8；`index.html` 大改 | 大 | D8 已否决，真要做得先回头改 PRD，不是技术选型能定的 |
| **加后端与数据库**（要跨设备同步） | 新增服务端代码与数据库；`PRD.md` 的 D4 / C2 / D2 / D10 全部推翻；本文档第 1、2、3 节重写 | **极大** | 等于重新立项，不是"改一下" |
| 换手机 / 换浏览器 | **没有文件受影响** —— 数据不会跟着走 | — | 风险 R3，MVP 接受，只能手工重录 |

### 从这张表能总结出的规律

| 你改的是 | 影响范围 |
| --- | --- |
| **样子**（配色、布局） | 只动 `index.html`，一天内随便改 |
| **数据放哪**（存储方式、字段） | 动 `PRD.md` + `index.html` + 本文档 —— 三处都要同步改，否则文档会骗人 |
| **数据去哪**（离开这台设备） | 等于推翻 D4/C2，**是重新立项，不是改需求** |

**这条规律现在是判断依据**：以后冒出新想法，先问它属于哪一类。属于第三类的，今天不该在 Day 5 的技术选型里讨论 —— 得回头走 PRD 的决策流程。

---
